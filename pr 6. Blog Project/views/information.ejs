<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Crazy Song Information</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #ff00ff, #ff6600, #00ffff);
      font-family: 'Comic Sans MS', cursive;
      text-align: center;
      padding: 20px;
      animation: bg-move 5s infinite alternate;
    }

    @keyframes bg-move {
      0% { background-position: left; }
      100% { background-position: right; }
    }

    h2 {
      font-size: 40px;
      color: #ffcc00;
      text-shadow: 3px 3px 10px #ff00ff;
      margin-bottom: 20px;
    }

    .container {
      background: rgba(0, 0, 0, 0.8);
      padding: 30px;
      border-radius: 20px;
      box-shadow: 0px 0px 20px #ff00ff;
      animation: bounce 2s infinite alternate;
    }

    @keyframes bounce {
      0% { transform: translateY(0px); }
      100% { transform: translateY(-10px); }
    }

    .list-group-item {
      background: #000;
      color: #ffcc00;
      font-size: 18px;
      border: 2px solid #ff00ff;
      border-radius: 10px;
      margin: 5px 0;
      padding: 15px;
    }

    img.cover {
      width: 200px;
      border-radius: 20px;
      box-shadow: 0px 0px 15px #ff00ff;
    }

    .btn-custom {
      display: inline-block;
      margin: 20px 10px;
      padding: 12px 25px;
      font-size: 18px;
      color: #fff;
      background: linear-gradient(45deg, #ff00ff, #ff6600);
      border-radius: 10px;
      text-decoration: none;
      transition: transform 0.3s ease;
    }

    .btn-custom:hover {
      transform: scale(1.1);
      box-shadow: 0px 0px 20px #ff00ff;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>🎵 Your Crazy Details 🎵</h2>
    <img src="<%= user.image %>" alt="User Image" class="cover">
    <ul class="list-group">
      <li class="list-group-item">🎤 Product Name: <%= user.name %></li>
      <li class="list-group-item">🆔 Product ID: <%= user.pid %></li>
      <li class="list-group-item">📜 Description: <%= user.description %></li>
      <li class="list-group-item">📍 Stock Location: <%= user.slocation %></li>
      <li class="list-group-item">⚖️ Unit: <%= user.unit %></li>
      <li class="list-group-item">📦 Stock Quantity: <%= user.sqty %></li>
    </ul>
    <a href="/deleteuser?id=<%= user.id %>" class="btn-custom">❌ Delete</a>
    <a href="/updateuser?id=<%= user.id %>" class="btn-custom">✏️ Edit</a>
  </div>
</body>
</html>
