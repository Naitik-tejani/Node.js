<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crazy Register UI</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: radial-gradient(circle, #ff00ff, #ff6600, #ffcc00);
            font-family: 'Comic Sans MS', cursive;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .login {
            background: rgba(0, 0, 0, 0.9);
            padding: 3rem;
            border-radius: 20px;
            box-shadow: 0px 0px 20px #ff00ff;
            text-align: center;
            animation: wiggle 2s infinite alternate;
        }

        @keyframes wiggle {
            0% { transform: rotate(-2deg); }
            100% { transform: rotate(2deg); }
        }

        h2 {
            color: #ffcc00;
            text-shadow: 2px 2px 5px #ff00ff;
            font-size: 2.5rem;
        }

        input {
            width: 80%;
            padding: 10px;
            border-radius: 10px;
            border: 3px solid #ff6600;
            font-size: 1.2rem;
            text-align: center;
            background: #000;
            color: #ffcc00;
            transition: 0.3s;
        }

        input:focus {
            border-color: #ff00ff;
            box-shadow: 0px 0px 15px #ff00ff;
        }

        button {
            margin-top: 1rem;
            padding: 12px 25px;
            border-radius: 10px;
            font-size: 1.5rem;
            font-weight: bold;
            border: none;
            background: linear-gradient(45deg, #ff00ff, #ff6600);
            color: #fff;
            box-shadow: 0px 0px 10px #ff00ff;
            transition: all 0.3s;
            cursor: pointer;
        }

        button:hover {
            transform: scale(1.1);
            box-shadow: 0px 0px 20px #ff00ff;
        }

        a {
            display: inline-block;
            margin-top: 10px;
            color: #ffcc00;
            text-decoration: none;
            font-size: 1.2rem;
        }

        a:hover {
            text-shadow: 0px 0px 10px #ff00ff;
        }
    </style>
</head>
<body>
    <div class="login">
        <h2>🚀 Welcome to Crazy Register 🚀</h2>
        <form action="/registeruser" method="post">
            <input type="text" name="name" placeholder="user name"  required>
            <br><br>
            <input type="email" name="email" placeholder="Email Address" required>
            <br><br>
            <input type="password" name="password" placeholder="Super Secret Password" required>
            <br><br>
            <button type="submit">💥 Register Now 💥</button>
        </form>
        <a href="/">Already have an account? Log in here!</a>
    </div>
</body>
</html>
